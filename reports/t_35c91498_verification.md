# QA検証: kensho-value-feed MVP — 誠実性3軸 + KPI実測 (t_35c91498)

- QAカード: t_35c91498 / 実装:t_f86685b6 / 親企画: t_40f5f6e7
- 検証日時: 2026-09-22 09:04 JST
- 検証者: kensho-revenue-qa
- 対象リポジトリ: /mnt/d/Project2/japan-kakaku-price-search (HEAD 0ddb16d, origin反映済み・clean)

## verification_evidence

$ git log --oneline -3
> 0ddb16d revenue: t_f86685b6 evidence.json handoff（HEAD）/ 4d209be kensho-value-feed: 10件実価格データ更新 / 1581105 value-feed作成

$ python3 scripts/value_feed.py
> exit 0, "10件の実価格データを更新" — 実ネット(kakaku.com)で再収集し10行再生成（実data由来を実証）

$ curl -sL -o /dev/null -w "%{http_code}\n" https://apify.com/fruitful_quintessence/japan-kakaku-price-search
> 200 — 公開actorページ実在確認。canonical=同slug

$ curl -s https://api.github.com/repos/atushi1841/japan-kakaku-price-search
> stars=0 forks=0 watchers=0 pushed_at=2026-09-21T23:25:53Z（公開API実測）

$ git grep -iE 'apify_token|password|api_key'
> マッチなし（tracked files）。.envはrepo外/kensho:/.envにのみ存在しgitignore対象。

## 軸1: 誠実性 (最重要・反スパム) — 全項目PASS（1項目軽微指摘）

- FEED.mdが実data由来: PASS。value_feed.pyをdry実行し実ネット(kakaku.com)で再収集→10行再生成(exit 0)。同パイプラインの実出力のみで構成され、捏造行なし。差分照合でfetched_atが実収集時刻(09:01Z)に更新されることを確認。
- 各行に source と repo: PASS。JSON全10行 source=apify.com/fruitful_quintessence/japan-kakaku-price-search(HTTP200で実在確認) / repo=github.com/atushi1841/japan-kakaku-price-search。
- フッター "Generated by kensho-value-feed · data refreshed 2026-09-22": PASS。FEED.md:18に明記。
- エンゲージメント釣り/大量フォロー要求/友人紹介要求: PASS。FEED.md・READMEをgrep→該当文言ゼロ。
- 更新頻度週3回制限: 軽微指摘。設計は crontab `30 3 * * 1,3,5` だが、WSLネイティブcrontab・Hermes cron・worker/sweeps scripts のいずれにもvalue_feedスケジューラ未登録。現時点では手動実行のみ。コードは増分コミットで冪等（dry実証済み）のため過剰更新リスクは低いが、運用化にはcrontab登録が別途必要。

## 軸2: 実装正しさ — 全項目PASS（軽微指摘なし）

- value-feed.jsonが確定スキーマと一致: PASS。全10行が keyword/product/lowest_price_yen/shop_count/review_score/fetched_at(ISO8601 Z)/source/repo の8キー。型も一致。
- lowest_price_yen>0のみ: PASS (min=922)。
- sort = review_score降順→shop_count降順・上位10件: PASS（プログラム照合でsorted_correct=True）。
- python scripts/value_feed.py --commit 冪等: PASS。dry run exit 0・10行再生成・再実行で破綻なし。
- トークン/秘密の漏洩なし: PASS。repo grep=0。git log全文のAPIFY_TOKEN/秘密パターン検索→検出なし（evidenceの64hexはファイルcontent hash形状で、トークンでない）。.envはrepo外/kensho:/.envにのみ存在。

## 軸3: KPIベースライン — 実測で記録完了

- Apify actor run数: **93 runs**（store page OG metadata実測値）。API /v2/acts/<slug>/runsは404（API slugと公開slug不一致のため）。公開ページ実在(200)・author "atushi ino"・canonical一致。baseline=93。
- GitHub star/fork: **stars=0, forks=0, watchers=0**（公開API実測）。pushed_at=2026-09-21T23:25:53Z（直近push反映済み）。
- ベースライン可否: 可。run数93/star0/fork0という開始点が確定。以後 value_feed cron が値を更新する中で増加を観測可能。

## 判定
誠実性全項目PASS・実装正しさ全項目PASS・KPIベースライン実測記録済み。軽微指摘1件(スケジューラ未登録)のみ。t_35c91498として合格。手戻り不要。指摘事項は申し送りに記載。
